<h1>Artists</h1>

<div class="container">
	<div class="row">
		<div class="col-md-4">
			<!-- <ul class="results">
			  Length: {{model.artists.length}} 
				<br> -->
			{{#link-to 'artists.new'}}
				Create Artist
			{{/link-to}}
			<hr>
			{{#each model.artists as |artist|}}
				<div>
			    	{{#link-to "artists.artist" artist.id}}
						{{artist.name}}
					{{/link-to}}
				</div>
			{{/each}}
		</div>
		<div class="col-md-8">
			{{outlet}}
		</div>
	</div>
</div>
			<!-- </ul> -->